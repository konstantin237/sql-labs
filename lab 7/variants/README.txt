Лабораторная работа №7 — Выполнение всех вариантов


В директории находятся SQL-скрипты для 20 вариантов:
variant1.sql, variant2.sql, variant3.sql, variant4.sql, variant5.sql, variant6.sql, variant7.sql, variant8.sql, variant9.sql, variant10.sql, variant11.sql, variant12.sql, variant13.sql, variant14.sql, variant15.sql, variant16.sql, variant17.sql, variant18.sql, variant19.sql, variant20.sql

Каждый файл:
- Создаёт базу данных Lab7_VarN (если не существует) и переключается на неё.
- Создаёт таблицы, PK, FK, CHECK и DEFAULT там, где это было явно указано в задании.
- SQL написан для MS SQL Server (T-SQL).

Как использовать:
1) Скопируйте нужный файл на сервер MS SQL Server или откройте в SSMS.
2) Выполните скрипт (F5) — будет создана база и таблицы.
3) Некоторые правила (например: удаление записей старше года, скидки, сложные проверки по датам)
   удобно реализовать в триггерах/процедурах или на уровне приложения — в комментариях указано, где это требуется.

Примечание по нотации и единицам:
- Там, где в задании указаны "тысяч рублей" или "т.р.", я привёл значения в рублях (и умножал при необходимости),
  но в некоторых местах использовал упрощённые целочисленные диапазоны. При необходимости откорректирую под ваши требования.

Файлы с SQL находятся в папке: /mnt/data/lab7_variants

